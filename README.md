# Emotify-tree-ensembles
Tree-based ensemble classifiers (Random Forest, Gradient Boosted Trees) for music emotion recognition on the Emotify dataset, benchmarked against existing SVM/KNN baselines using the GEMS-9 emotion model.
